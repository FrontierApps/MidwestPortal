
  <form>
  <%= select_tag "company", options_from_collection_for_select(@companies,"id", "company"), id: "simple-example" %>
  </form>

  <!-- <table>
    <tr>
      <th>Company</th>
    </tr>
    <% @companies.each do |company| %>
      <tr>
        <td><%= link_to company.company, company_path(company)%></td>
        
      </tr>
    <% end %>
  </table>
 -->